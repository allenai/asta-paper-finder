type NaturalLanguageText = str
type MessageId = str
type Timestamp = float
# asta websurface uses corpus id as int so for compatability we represent it the same in widgets
type LegalToShow = bool
type ResultSetId = str
type ResultSetSliceId = str
